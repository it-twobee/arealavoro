// Client Klaviyo API: è lib/tracking/klaviyo.ts, condiviso col CRM.
// Contesto delle chiamate: `{ apiKey, endpoints? }`, con `defaultEndpoints()`
// che legge anche la revisione API (TWOBEE_KLAVIYO_REVISION).
export * from '../lib/tracking/klaviyo.ts';
